# Требования:
`python 3.8+`
`numpy 2.3`
`matplotlib 3.10`

### Как использовать короче:
* В функцию function вписываем нужное нам уравнение по правилам питончика
* При вызове solve в параметрах прописываем, чему должно быть равно уравнение (target_y) и диапазон в котором ищется решение (start, stop, step)
